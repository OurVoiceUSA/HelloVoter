import { expect } from 'chai';

import * as App from '../src/App';

describe('Test', function () {

  before(async () => {
  });

  after(async () => {
  });

  it('App renders', async () => {
    let a = new App.default();
  });

});
